import { GoogleGenAI } from '@google/genai';\nimport { LessonMemo } from '../types';\nimport { SmartDocumentSource, buildDocumentContext } from './smartDocuments';\n\nexport type SmartAiMode = 'chat' | 'solve' | 'assessment' | 'exam' | 'lesson' | 'web';\n\nexport interface SmartAiRequest {\n  question: string;\n  lesson?: LessonMemo | null;\n  curriculum: LessonMemo[];\n  documents?: SmartDocumentSource[];\n  mode?: SmartAiMode;\n  useWebSearch?: boolean;\n}\n\nexport interface SmartAiResponse {\n  text: string;\n  sources: Array<{ title: string; uri: string }>;\n  usedWebSearch: boolean;\n}\n\nconst compact = (value: unknown, max = 1800): string => {\n  const text = typeof value === 'string' ? value : JSON.stringify(value ?? '');\n  return text.length > max ? text.slice(0, max) + '…' : text;\n};\n\nconst buildCurriculumContext = ({ lesson, curriculum }: SmartAiRequest) => {\n  const level = lesson?.level;\n  const sameLevel = level ? curriculum.filter(item => item.level === level) : curriculum;\n  const sameMaqta = lesson?.maqta ? sameLevel.filter(item => item.maqta === lesson.maqta) : [];\n  const relevantLessons = (lesson ? [lesson, ...sameMaqta.filter(x => x !== lesson)] : sameLevel)\n    .slice(0, 16).map(item => ({\n      sourceLearningUnitId: item.sourceLearningUnitId, sourceResourceId: item.sourceResourceId,\n      level: item.level, midan: item.midan, maqta: item.maqta, mawrid: item.mawrid, ta3alom: item.ta3alom,\n      markaba: item.markaba, kafaaKhitamiya: item.kafaaKhitamiya, ma3ayirTaqwim: item.ma3ayirTaqwim,\n      marifa: item.marifa, manhaji: item.manhaji, wadiya: item.wadiya, moshkila: item.moshkila,\n      faradiyat: item.faradiyat, irsae: item.irsae, taqwim: item.taqwim,\n      activities: item.anshita.map(a => ({ sourceActivityId: a.sourceActivityId, title: a.title, asila: compact(a.asila, 1200), ajwiba: compact(a.ajwiba, 1200) })),\n    }));\n  return JSON.stringify({ currentLesson: lesson || null, levelCount: sameLevel.length, sameMaqtaCount: sameMaqta.length, relevantLessons }, null, 2);\n};\n\nconst modeInstruction = (mode: SmartAiMode = 'chat') => {\n  switch (mode) {\n    case 'solve': return 'حلّل النشاط أو المشكلة خطوة بخطوة، ثم أعط الإجابة النموذجية، واذكر بوضوح ما هو مستخرج من النشاط وما هو اقتراح.';\n    case 'assessment': return 'أنشئ تقويماً مرتبطاً مباشرة بالمورد والأنشطة: وضعيات، أسئلة، معايير ومؤشرات، ثم التصحيح النموذجي. لا تضف مورداً غير موجود دون وسمه كاقتراح.';\n    case 'exam': return 'أنشئ فرضاً أو اختباراً مدرسياً جاهزاً للطباعة انطلاقاً من الأنشطة المحددة في السياق، مع تنويع الأسئلة والتدرج في الصعوبة والتصحيح النموذجي وسلم التنقيط.';\n    case 'lesson': return 'ساعد الأستاذ في بناء الحصة: وضعية انطلاق، مشكل، فرضيات، مسعى تقصي، استثمار النتائج، إرساء وتقويم، مع احترام تسلسل المورد.';\n    case 'web': return 'ابحث في الإنترنت عند الحاجة، وميّز المعلومات الخارجية عن محتوى المنهاج. اعرض مصادر الويب التي بنيت عليها المعلومات.';\n    default: return 'أجب عن سؤال الأستاذ مع إعطاء الأولوية لمصادر المنصة والمصادر المحلية المرفوعة.';\n  }\n};\n\nexport async function askSmartAi(request: SmartAiRequest): Promise<SmartAiResponse | null> {\n  const apiKey = import.meta.env.VITE_GEMINI_API_KEY;\n  if (!apiKey) return null;\n  try {\n    const ai = new GoogleGenAI({ apiKey });\n    const model = import.meta.env.VITE_GEMINI_MODEL || 'gemini-2.5-flash';\n    const docs = request.documents || [];\n    const documentContext = buildDocumentContext(docs, 30000);\n    const useWebSearch = request.useWebSearch !== false || request.mode === 'web';\n    const prompt = [\n      'أنت المساعد البيداغوجي الذكي لمنصة ميزانياتي لأساتذة علوم الطبيعة والحياة في التعليم المتوسط بالجزائر.',\n      'قاعدة المصدر والأمان المعرفي:',\n      '1. قاعدة المنهاج الموجودة في المنصة هي المصدر الأول عند السؤال عن الميادين والمقاطع والموارد والأنشطة.',\n      '2. الوثيقة المرافقة والمذكرات والملفات المحلية مصادر مرجعية إضافية، ولا تنسب معلومة إليها إلا إذا كانت موجودة فعلاً في السياق.',\n      '3. لا تخترع نشاطاً أو مورداً رسمياً. أي إضافة جديدة اكتب قبلها: اقتراح تربوي.',\n      '4. عند وجود تعارض بين مصدرين، اذكر التعارض ولا تحسمه باختراع معلومة.',\n      '5. عند استخدام الويب، اعتبره مصدراً خارجياً، ولا تجعله بديلاً عن الوثائق الرسمية المحلية.',\n      '6. عند إعداد فرض أو اختبار، اربط كل سؤال بالمورد أو النشاط الذي بُني عليه قدر الإمكان.',\n      '7. أجب بالعربية الواضحة وبمصطلحات المنهاج الجزائري، مع عناوين ونقاط واضحة.',\n      '', 'المهمة الحالية:', modeInstruction(request.mode),\n      '', 'سياق المنهاج:', buildCurriculumContext(request),\n      '', docs.length ? 'المصادر المحلية المرفوعة:' : 'لا توجد ملفات محلية إضافية مرفوعة.',\n      documentContext, '', 'طلب الأستاذ:', request.question,\n    ].join('\n');\n    const response = await ai.models.generateContent({ model, contents: prompt, config: useWebSearch ? { tools: [{ googleSearch: {} }] } : undefined });\n    const text = response.text?.trim();\n    if (!text) return null;\n    const chunks = (response.candidates?.[0] as any)?.groundingMetadata?.groundingChunks || [];\n    const sources = chunks.map((chunk: any) => chunk?.web).filter((web: any) => web?.uri)\n      .map((web: any) => ({ title: web.title || web.uri, uri: web.uri }))\n      .filter((source: { title: string; uri: string }, index: number, arr: { title: string; uri: string }[]) => arr.findIndex(item => item.uri === source.uri) === index);\n    const sourceText = sources.length ? '\n\n**مصادر الويب المستخدمة:**\n' + sources.map(s => '• ' + s.title + '\n  ' + s.uri).join('\n') : '';\n    return { text: text + sourceText, sources, usedWebSearch: sources.length > 0 };\n  } catch (error) { console.error('[smart-ai] generation failed', error); return null; }\n}