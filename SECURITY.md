# 🔐 سياسة الأمان

## قواعد أساسية

- لا تضع مفاتيح Firebase أو Gemini الحقيقية في Git.
- استخدم `.env.local`، واحتفظ بـ `.env.example` للقيم الوهمية فقط.
- راجع `firestore.rules` قبل كل نشر.
- بيانات المستخدمين معزولة حسب `request.auth.uid`.
- عند تسريب مفتاح، أبطله وأصدر مفتاحاً جديداً من لوحة الخدمة فوراً.

## التحقق قبل النشر

```bash
npm run lint
npm run build
firebase deploy --only firestore:rules
```

> مفاتيح Firebase الخاصة بتطبيقات الواجهة ليست بديلاً عن قواعد الأمان؛ الحماية الفعلية تعتمد على قواعد Firestore وعدم استخدام مفاتيح خادم في الواجهة.
