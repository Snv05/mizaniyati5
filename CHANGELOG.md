# CHANGELOG.md

## [Unreleased] - 2026-09-16

### Fixed
- إصلاح وصف `metadata.json` المقطوع.
- تصحيح `allowedHosts` في `vite.config.ts`.
- إضافة قواعد Firestore لمجموعات `data` و`config` و`distributions` و`dailyLogs`.
- تشديد عزل بيانات المستخدمين والتحقق من حقل `userId`.
- تحسين حماية ملفات البيئة في `.gitignore`.

### Documentation
- إضافة سياسة الأمان `SECURITY.md`.
